# DLADS Rule Engine — threshold configuration
# Edit these without recompiling.

[ssh_brute_force]
ssh_threshold   = 5
ssh_window_s    = 30

[privilege_escalation]
priv_lookback_s = 86400

[port_scan]
port_threshold  = 10
port_window_s   = 60

[deduplication]
cooldown_s      = 60